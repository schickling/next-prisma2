# Migration `20191216082439-init`

This migration has been generated by schickling at 12/16/2019, 8:24:39 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "next"."User" (
  "age" integer   ,
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "lastName" text NOT NULL DEFAULT '' ,
  "name" text   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "next"."Post" (
  "content" text   ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "published" boolean NOT NULL DEFAULT true ,
  "title" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "next"."Post" ADD COLUMN "author" text   REFERENCES "next"."User"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "User.email" ON "next"."User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20191216082439-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,28 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://prisma:qd58rcCywPRS4Stk@playground-database-postgres.cluster-clfeqqifnebj.eu-west-1.rds.amazonaws.com:5432/postgres?schema=next"
+}
+
+model User {
+  id       String  @default(cuid()) @id
+  email    String  @unique
+  name     String?
+  // test
+  lastName String
+  posts    Post[]
+  age      Int?
+}
+
+model Post {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean  @default(true)
+  title     String
+  content   String?
+  author    User?
+}
```


